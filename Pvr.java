class Pvr {
    
    public static int searchMoviePriceByName(String movieName) {
        
        int moviePrice;
        
        if (movieName == "Indian 2") return moviePrice = 160;
        
        if (movieName == "Kalki 2898 AD") return moviePrice = 290;
        
        if (movieName == "Deadpool and Wolverine") return moviePrice = 350;
        
        if (movieName == "Safira") return moviePrice = 149;
        
        if (movieName == "Despicable me 4") return moviePrice = 249;
        
        if (movieName == "Kill") return moviePrice = 220;
        
        if (movieName == "Chandu Champion") return moviePrice = 112;
        
        if (movieName == "Inside Out 2") return moviePrice = 150;
        
        if (movieName == "Love Li") return moviePrice = 100;
        
        if (movieName == "Munjya") return moviePrice = 199;
        
        if (movieName == "Maharaja") return moviePrice = 120;
        
        if (movieName == "KGF") return moviePrice = 300;
        
        if (movieName == "Ugramm") return moviePrice = 150;
        
        if (movieName == "Salaar") return moviePrice = 250;
        
        if (movieName == "Animal") return moviePrice = 200;
        
        if (movieName == "Godzilla x Kong") return moviePrice = 250;
        
        if (movieName == "power") return moviePrice = 180;
        
        if (movieName == "RRR") return moviePrice = 200;
        
        if (movieName == "Devara") return moviePrice = 250;
        
        if (movieName == "Aavesham") return moviePrice = 100;
        
        else
            System.out.println(movieName + " not found");
        return 0;
    }
    
    public static int searchMoviePriceByName(String movieName, int numberOfPerson) {
        
        int moviePrice;
        
        if (movieName == "Indian 2") return moviePrice = 160 * numberOfPerson;
        
        if (movieName == "Kalki 2898 AD") return moviePrice = 290 * numberOfPerson;
        
        if (movieName == "Deadpool and Wolverine") return moviePrice = 350 * numberOfPerson;
        
        if (movieName == "Safira") return moviePrice = 149 * numberOfPerson;
        
        if (movieName == "Despicable me 4") return moviePrice = 249 * numberOfPerson;
        
        if (movieName == "Kill") return moviePrice = 220 * numberOfPerson;
        
        if (movieName == "Chandu Champion") return moviePrice = 112 * numberOfPerson;
        
        if (movieName == "Inside Out 2") return moviePrice = 150 * numberOfPerson;
        
        if (movieName == "Love Li") return moviePrice = 100 * numberOfPerson;
        
        if (movieName == "Munjya") return moviePrice = 199 * numberOfPerson;
        
        if (movieName == "Maharaja") return moviePrice = 120 * numberOfPerson;
        
        if (movieName == "KGF") return moviePrice = 300 * numberOfPerson;
        
        if (movieName == "Ugramm") return moviePrice = 150 * numberOfPerson;
        
        if (movieName == "Salaar") return moviePrice = 250 * numberOfPerson;
        
        if (movieName == "Animal") return moviePrice = 200 * numberOfPerson;
        
        if (movieName == "Godzilla x Kong") return moviePrice = 250 * numberOfPerson;
        
        if (movieName == "power") return moviePrice = 180 * numberOfPerson;
        
        if (movieName == "RRR") return moviePrice = 200 * numberOfPerson;
        
        if (movieName == "Devara") return moviePrice = 250 * numberOfPerson;
        
        if (movieName == "Aavesham") return moviePrice = 100 * numberOfPerson;
        
        else
            System.out.println(movieName + " not found");
        return 0;
    }
}
